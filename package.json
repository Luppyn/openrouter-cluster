{
  "name": "openrouter-cluster",
  "version": "1.0.0",
  "description": "OpenRouter API cluster with race-based deduplication. Dispatch requests to multiple API keys and return the first response.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "openrouter",
    "api-cluster",
    "race",
    "load-balancer",
    "proxy"
  ],
  "license": "MIT",
  "dependencies": {
    "fastify": "^5.2.0",
    "@fastify/static": "^8.0.0",
    "dotenv": "^16.4.0"
  }
}
